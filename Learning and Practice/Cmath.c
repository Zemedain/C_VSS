#include <stdio.h>
#include <math.h>

int main(){

    short A = sqrt(81);
    int B = pow(2, 9);
    short C = round(43.5736);
    short D = ceil(3.0000001);
    short E = floor(4.9999999);
    int F = fabs(-1239000);
    double G = log(5);
    double H = sin(30);
    double J = cos(60);
    double I = tan(45);

    printf("%lf\n",J);

    return 0;

}