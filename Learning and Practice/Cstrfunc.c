#include <stdio.h>
#include <string.h>

int main(){

    char string1[] = "Test";
    char string2[] = "string";

//  strlwr(string1);
//  strupr(string1);
//  strcat(string1, string2);    // concatenate string 1 and 2
//  strncat(string1, string2, 3);  // concatenate n characters from string 2 to 1
//  strcpy(string1, string2);    // replaces string 1 with 2
//  strncpy(string1, string2, 3);  // replaces n characters in string 1 with 2 from left to right
//  strset(string1, 'E');        // replaces all of string 1 with specified character
//  strnset(string1, 's', 1);    // replaces n characters of string 1 with specified character
//  strrev(string1);             // reverses string 1

//  short result = strlen(string1);  // returns the length of string1 in numbers
//  short result = strcmp(string1, string2);  // returns 0 when string 1 and 2 are the same
//  short result = strncmp(string1, string2, 2);  // returns 0 if n characters of string 1 and 2 are the same
//  short result = strcmpi(string1, string2);  // ignores case sensitivity
//  short result = strnicmp(string1, string2);  // ignores case sensitivity of n characters

//  printf("%d",result);

/*  if(result == 0){
        printf("These strings are the same.");
    
    }
    else{
        printf("These strings are not the same");
    } */

    return 0;

}